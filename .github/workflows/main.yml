name: Deloy to caprover
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
    - uses: AlexxNB/caprover-action@v1
      with:
        server: https://captain.caprover-root.subz.fun/
        password: pampeliska
        appname: authier-api
        branch: github-actions
