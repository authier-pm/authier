query SyncSettings {
  me {
    PasswordLimits
    TOTPlimit
    id
    autofill
    language
    theme
  }
  currentDevice {
    id
    syncTOTP
    vaultLockTimeoutSeconds
  }
}

mutation updateSettings($config: SettingsInput!) {
  me {
    updateSettings(config: $config) {
      id
    }
  }
}
