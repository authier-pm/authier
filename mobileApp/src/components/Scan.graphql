mutation addDevice(
  $userId: String!
  $name: String!
  $firstIpAdress: String!
  $firebaseToken: String!
) {
  addDevice(
    userId: $userId
    name: $name
    firstIpAdress: $firstIpAdress
    firebaseToken: $firebaseToken
  )
}
