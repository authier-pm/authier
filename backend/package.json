{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^2.30.3",
    "@react-native-firebase/app": "^12.7.4",
    "@types/jsonwebtoken": "^8.5.5",
    "bcrypt": "^5.0.1",
    "chalk": "^4.1.2",
    "class-validator": "^0.13.1",
    "cpy": "^8.1.2",
    "dotenv": "^10.0.0",
    "fastify": "^3.20.2",
    "fastify-cookie": "^5.3.1",
    "fastify-jwt": "^3.0.1",
    "firebase-admin": "^9.11.1",
    "graphql": "^15.5.2",
    "graphql-fields": "^2.0.3",
    "graphql-scalars": "^1.10.0",
    "jsonwebtoken": "^8.5.1",
    "mercurius": "^8.1.3",
    "prisma": "2.30.3",
    "reflect-metadata": "^0.1.13",
    "ts-node": "^10.2.1",
    "type-graphql": "^1.1.1",
    "typescript": "^4.4.2"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "jest": "^27.1.0",
    "mz": "^2.7.0",
    "nodemon": "^2.0.12",
    "ts-jest": "^27.0.5",
    "typegraphql-prisma": "npm:@capaj/typegraphql-prisma@^0.14.11"
  },
  "scripts": {
    "dev": "nodemon --exec ts-node --transpile-only server.ts",
    "local": "nodemon server.ts",
    "pgn": "prisma migrate dev && prisma db push && prisma generate",
    "gbs": "ts-node-transpile-only scripts/generateGqlSchemas.ts",
    "migrate": "prisma migrate deploy",
    "build": "rm -rf dist && tsc && node scripts/build.js && yarn tar",
    "deploy": "caprover deploy -t authier-api.tar.gz",
    "buildAndDeploy": "yarn build && yarn deploy",
    "DbPush": "prisma db push",
    "generate": "prisma generate",
    "postinstall": "prisma generate",
    "test": "jest",
    "tsc": "tsc",
    "start": "prisma migrate deploy && prisma generate && node server.js",
    "tar": "tar -czvf authier-api.tar.gz -C ./dist ."
  }
}
